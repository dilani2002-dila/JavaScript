function finduniqueNumbers (arr) {
                let uniqueNumber=[];
                uniqueNumber[0]=arr[0];
                for (let i=0; i <uniqueNumber.length; i++) {
                for(let j=0; j<arr.length; j++) {
                if(uniqueNumber[i==arr[j]])
                }
                }
                }
