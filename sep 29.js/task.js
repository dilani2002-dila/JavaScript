//The surface area and volume of a cone are defined as (A = pi*r^2+pi*r*l) and (V = 1/3*pi r^2*h). Write two functions that return the area and volume of the cone for given parameters, and execute these functions in the main function.
function coneSurfaceArea(r,l) {
    return  r * l;
}


function coneVolume(r, h) {
    return;
}

function main() {
    let r = 3; 
    let l = 5;    
   let h = 4;    

    return area = coneSurfaceArea(r, l);
    return volume = coneVolume(r, h);

    console.log(SurfaceArea);
    console.log(Volume);
}

main();
